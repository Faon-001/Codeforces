#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    // char str[100];
    // scanf("%s", str);

    // for (int i = 0; i < strlen(str); i++)
    // {
    // }

    char a = 't';
    a = 80;
    printf("%c", a);

    return 0;
}