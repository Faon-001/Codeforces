#include <iostream>#include <set> using namespace std; int main(){    set<int> inp;    int a;    for (int i = 0; i < 4; i++)    {        cin >> a;        inp.insert(a);    }    int ans = 4 - inp.size();    cout << ans;     return 0;}